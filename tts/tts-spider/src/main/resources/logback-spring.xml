<?xml version="1.0" encoding="UTF-8"?>
<configuration>
  <property resource="application.properties"/>
  <springProperty scope="context" name="TEST_FILE_PATH" source="log.path"/>

    <!-- 文件输出格式 -->
<!--     <property name="PATTERN" value="%-12(%d{yyyy-MM-dd HH:mm:ss.SSS}) |-%-5level [%thread] %c [%L] -| %msg%n"/>
 -->    
     <property name="PATTERN" value="%-12(%d{yyyy-MM-dd HH:mm:ss.SSS}) -| %msg%n"/>
    
    
    <!-- test文件路径 -->
    <!-- <property name="TEST_FILE_PATH" value="${log.path}"/> -->

    <!-- 开发环境 -->
    <springProfile name="dev">
        <!-- 每天产生一个文件 -->
        <appender name="DEV-FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <!-- 文件路径 -->
            <file>${TEST_FILE_PATH}/tts.log</file>
            <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
                <!-- 文件名称 -->
                <fileNamePattern>${TEST_FILE_PATH}/tts.%d{yyyy-MM-dd}.log</fileNamePattern>
                <!-- 文件最大保存历史数量 -->
                <MaxHistory>100</MaxHistory>
            </rollingPolicy>

            <layout class="ch.qos.logback.classic.PatternLayout">
                <pattern>${PATTERN}</pattern>
            </layout>
        </appender>

        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>${PATTERN}</pattern>
            </encoder>
        </appender>

        <logger name="com.yh.bigdata.tts.common.dao" additivity="true">
            <level value="DEBUG" />
        </logger>
<!--         <logger name="org.springframework.web" additivity="true">
            <level value="INFO" />
        </logger>
        <logger name="org.quartz" additivity="true">
            <level value="WARN" />
        </logger> -->

        <root level="info">
            <appender-ref ref="DEV-FILE"/>
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>

</configuration>
